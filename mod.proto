syntax = "proto3";

package mod;

service ModService {
    rpc TestMod(TestModReques) returns (TestModResponse);
}

message TestModRequest {
    int64 mod_id = 1;
}

message TestModResponse {
    bool success = 1;
}
