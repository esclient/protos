syntax = "proto3";

package mod;

service ModService {
    rpc TestMod(TestModRequest) returns (TestModResponse);
}

message TestModRequest {
    int64 mod_id = 1;
}

message TestModResponse {
    bool success = 1;
}
